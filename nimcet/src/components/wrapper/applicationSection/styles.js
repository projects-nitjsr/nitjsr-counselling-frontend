import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  heading: {
    padding: "8px",
    background: "black",
    color: "white",
  },
}));

export default useStyles;
